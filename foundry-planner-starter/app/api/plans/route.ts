import { NextResponse } from 'next/server';import { getSupabaseAdmin } from '@/lib/supabaseAdmin';export async function POST(req:Request){try{const body=await req.json();const edit_key=crypto.randomUUID();const supabase=getSupabaseAdmin();const{data,error}=await supabase.from('plans').insert({title:body.title||'Foundry Plan',player_input:body.player_input||'',plan_json:body.plan_json||{},settings_json:body.settings_json||{},edit_key}).select('id, edit_key').single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}